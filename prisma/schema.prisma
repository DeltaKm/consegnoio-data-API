
// Index:
//  objType    r17  
//  enumType   r164  
//  dataModel  r236

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}


// ***** Inizio objType *****
type CordType {
  lat Float
  long Float
}
// Token metaData standard ERC20 => da implementare in model
type DeliveryValidation {
  ritiro Boolean
  consegna Boolean
}

type TokenType {
  tokenName String
  tokenSumbol String
  decimalPlaces Int
  totalSupply Float
  eurValue Float
  }

type DatiFiscaliType {
  codiceFisclale String
  ragioneSociale String
  partitaIVA String
  codiceSDI String
  pec String
}

type IndirizzoType {
  via String
  numeroCivio String
  provincia String
  provinciaSigla String
  citta String
  cap Int
}

type AnagraficaUserType {
  nome String
  cognome String
  domicilio IndirizzoType
  residenza IndirizzoType 
  cittadinanza CittadinanzaEnum
  fiscalita DatiFiscaliType
  tel String
}

type PosMapType {
  coordinate CordType
  indirizzo IndirizzoType
  }

type CostoType {
  freeDelivery Boolean
  quota Float
  pagamento metodoPagamento
}

type DatiMerceType {
  tipoMerce CategoriaMerceEnum
  nome String
  peso Float
  altezza Float
  larghezza Float
  lunghezza Float
  densita Float
  pesoVolumetrico Float
}

type PreferenzeZonaRaiderType {
  zoneConsegna String[]
  distanzaKM Int
}

// prototipo
type WorkTime {
  data DateTime
  oreTotali Float
}

type RouteModifiersType {
  avoidTolls Boolean
  avoidHigways Boolean
  avoidFerries Boolean
  avoidIndoor Boolean  
}

// ----- INIZIO DistanceMatrixType vTEST ----
type Distance {
  text String
  value Float
}

type Duration {
  text String
  value Float
}
type gWaypoint {
  location CordType
  routeMod Boolean
 }
//unitParam: imperial= miles / yard, metric=km / meters
type DistanceMatrixType {
  destinazione String
  origine String
  statusDO String
  distanza Distance
  durata Duration
  statusDD String
}
// ----- FINE DistanceMatrixType vTEST  ----

// ----- INIZIO GeocodingType vTEST ----
type PulsCodeType {
  compoundCode String
  globalCode String
}
type GoogleAddressComponentType {
  formattedAddress String
  cord CordType
  palceId String
  code PulsCodeType
}
// ----- FINE GeocodingType vTEST  ----

// ----- INIZIO DirectionType vTEST ----
//prototipo
type DirectionType {
origin CordType
destination CordType
travelMode MetodoConsegnaEnum
transitOptions String
drivingOptions String
unitSystem String
waypoints String[]
optimizeWaypoints Boolean
provideRouteAlternatives Boolean
avoidFerries Boolean
avoidHigways Boolean
avoidTolls Boolean
region String
}
// ----- FINE DirectionType vTEST  ----

// ***** Fine objType *****



// ***** Inizio enumType *****
enum metodoPagamento {
  cashOnDelivery
  paid  
}

enum StatusCodes {
  NotFound
}
enum CittadinanzaEnum {
  Italiana
  Estera
}

enum CategoriaMerceEnum {
  Alimentare
  MaterialiVari
}

enum StatoDisponibilitaEnum {
  Disponibile
  Occupato
}
//da effettuare il refactor per google
enum MetodoConsegnaEnum {
  Driving  
  Walking
  Bicycling
}

enum GoogleRouteTravelModeEum {
  TRAVEL_MODE_UNSPECIFIED //VALORE PREDEFINITO SE NON SPECIFICATO "DRIVE"
  DRIVE
  BICYCLE
  WALK 
  TWO_WHEELER // modalità due ruote a motore
  TRANSIT // percorsi mezzi pubblici
}

enum GoogleRoutingPreference {
  ROUTING_PREFERENCE_UNSPECIFIED // Nessuna preferenza di routing specificata. Il valore predefinito è TRAFFIC_UNAWARE.
  TRAFFIC_UNAWARE // Calcola i percorsi senza prendere in considerazione le condizioni del traffico in tempo reale. Ideale quando le condizioni del traffico non sono importanti o non sono applicabili. L'uso di questo valore produce la latenza più bassa. Nota: per RouteTravelMode DRIVE e TWO_WHEELER, il percorso e la durata scelti si basano sulla rete stradale e sulle condizioni medie del traffico non dipendenti dal tempo, non sulle condizioni delle strade attuali. Di conseguenza, i percorsi potrebbero includere strade temporaneamente chiuse. I risultati di una determinata richiesta possono variare nel tempo a causa di cambiamenti della rete stradale, condizioni medie aggiornate del traffico e natura distribuita del servizio. Inoltre, i risultati possono variare tra percorsi quasi equivalenti in qualsiasi momento o frequenza.
  TRAFFIC_AWARE // Calcola i percorsi prendendo in considerazione le condizioni del traffico in tempo reale. A differenza di TRAFFIC_AWARE_OPTIMAL, vengono applicate alcune ottimizzazioni per ridurre significativamente la latenza.
  TRAFFIC_AWARE_OPTIMAL // Calcola i percorsi prendendo in considerazione le condizioni del traffico in tempo reale, senza applicare la maggior parte delle ottimizzazioni delle prestazioni. L'uso di questo valore produce la massima latenza.
}

enum RuoloEnum {
  SuperAdmin
  Raider 
  Contractor
  RaiderContractor
  BusinessAdmin
}

enum StatoConsegnaEnum {
  daRitirare
  inConsegna
  Consegnato
  Cancellato
}

enum presetMesagge {
  Type1
  Type2
  Type3
  Type4
  Type5
}
// ***** Fine enumType *****



// ***** Inizio dataModel *****
model User {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    idSlug String
    email String
    tel String
    profileImg String @default("/asset/img/logoconsegnoio.svg")
    // image String?
    // accounts Account[]
    // sessions Session[]
    // Authenticator Authenticator[]
    username String 
    password String
    // role RuoloEnum  <= da implementare dopo i reference
    // raiderId Raider?
    // contractorId Contractor?
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    @@unique([idSlug, email, username]) 
}
 
model Account {
  id                String  @id @default(auto()) @map("_id") @db.ObjectId
  userId            String  @db.ObjectId
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.String
  access_token      String? @db.String
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.String
  session_state     String?
 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
 
  // user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 
  @@unique([provider, providerAccountId])
}
 
model Session {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  sessionToken String   @unique
  userId       String   @db.ObjectId
  expires      DateTime
  // user         User     @relation(fields: [userId], references: [id], onDelete: Cascade) 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
 
model VerificationToken {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  identifier String
  token      String
  expires    DateTime
 
  @@unique([identifier, token])
}
 

model Authenticator {
  credentialID         String  @id @map("_id")
  userId               String  @db.ObjectId
  providerAccountId    String
  credentialPublicKey  String
  counter              Int
  credentialDeviceType String
  credentialBackedUp   Boolean
  transports           String? 
  // user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 
  @@unique([userId, credentialID])
}

model Raider{
 id String @id @default(auto()) @map("_id") @db.ObjectId
 metodo MetodoConsegnaEnum
 stato StatoDisponibilitaEnum
 preferenze PreferenzeZonaRaiderType
 isOnWorkFlag Boolean // attivo se il raider ha preso in carico una consegna
 raiderPosition CordType
 storicoConsegne String[] // add reference to Delivery 
 workTime Float //da definire per i filtri
 preferiti String[] 
 blacklist String[]
 rating Float
 createdAt DateTime @default(now())
 updateAt DateTime @updatedAt    
}

model Contractor {
  id String @id @default(auto()) @map("_id") @db.ObjectId
   // user User @relation(fields: [userId], references: [id])
  userId String @unique @db.ObjectId
  orderId Order?
  posizioneSede PosMapType[] // indirizzi sede
  storicoConsegne String[] // add reference to Delivery
  preferitiId String[]
  blacklistId String[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt            
}

model Order {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  orderId String
  user Contractor @relation(fields: [contractorId], references: [id])
  contractorId String @unique @db.ObjectId
  hashQrFirma String
  dataOreRitiroPrevisto DateTime
  dataOreConsegnaPrevista DateTime
  indirizzoRitiro PosMapType
  indirizzoConsegna PosMapType
  distanza String
  note String
  quotaRaider Float
  quota CostoType
  identificaitvoMerce DatiMerceType
  isActive Boolean
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  }

  model Delivery {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    raiderID String // add reference to Raider 
    ordine String // add reference to Order 
    stato StatoConsegnaEnum
    dataOraRitiroReale DateTime
    dataOraConsegnaReale DateTime
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model GooglePosMap {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    placeId String @unique
    indirizzo PosMapType 
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }
  
  model DeletedDelivery {
        id String @id @default(auto()) @map("_id") @db.ObjectId
        fromID String // add reference to User
        fromRuolo RuoloEnum 
        createdAt DateTime @default(now())
        updateAt DateTime @updatedAt 
  }

  
  model DeletedOrder {
        id String @id @default(auto()) @map("_id") @db.ObjectId
        contractorID String // add reference to Contractor
        createdAt DateTime @default(now())
        updateAt DateTime @updatedAt
  }

  // prototipo
  model StoricoConsegne {
     id String @id @default(auto()) @map("_id") @db.ObjectId 
     idDelivery String[] // add reference to Delivery
     createdAt DateTime @default(now())
     updateAt DateTime @updatedAt
  }

  model Comuni {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    codiceIstat String
    denominazioneIta String
    cap String
    siglaProvincia String
    denominazioneProvincia String
    denominazioneRegione String
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model Nazioni {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    siglaNazione String
    codiceBelfiore String
    denominazioneNazione String
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  // prototipo
  model RaiderWorkTime {
        id String @id @default(auto()) @map("_id") @db.ObjectId
        hours WorkTime
        cratedAt DateTime @default(now())
        updateAt DateTime @updatedAt
  }

  // prototipo google DistanceMatrix => wsRouteDistanceService
  model DistanceMatrix {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    matrix DistanceMatrixType
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

// prototipo google Gecoding => wsGecodingService
  model Geocoding {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    googleAddress GoogleAddressComponentType
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt 
  }
  // prototipo
  model Customer { 
    id String @id @default(auto()) @map("_id") @db.ObjectId
    name String
    tel String
    address PosMapType
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }
  // prototipo
  model CustomerNote {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    note String
    raiderId String // add reference
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }
  // prototipo
  model Wallet {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    userId String // reference
    token TokenType
    tokenAmmount Float 
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }
// prototipo raider profile
  model ViewRaider {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    raiderId String // reference
    numeroConsegne Int
    areaConsegna String[]
    rating String // reference
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }
// prototitpo contractor profile
  model ViewContractor {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    contractorId String //reference
    areaCosnegna String[]
    rating String // reference
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model CorsConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    methods String []
    cotent String @db.ObjectId
    log String @db.ObjectId
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model CoreConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    mobileClientParam String @db.ObjectId
    dashBoardParam String  @db.ObjectId
    seviceParam String @db.ObjectId
    cloudParam String @db.ObjectId
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model VercelConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    amazonUrl String
    googleUrl String
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

// prototipo
  model ClientDashBoardConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    path String[] @db.ObjectId
    crertedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

// protitipo
  model ClientMobileConfig {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  path String[] @db.ObjectId
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
}

// prototipo chat rapida Contractor <=> Raider
model ChatLog {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  presetMesagge String [] @db.ObjectId
  textMessage String [] @db.ObjectId
  raiderId String @db.ObjectId // reference
  contractorId String @db.ObjectId // reference
  ttl DateTime
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
}

// protitipo
model FirebaseNotification {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  config String [] @db.ObjectId
  message String [] @db.ObjectId
  schedule String [] @db.ObjectId
  ttl DateTime
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
 }

 model FirebaseRaiderNotification {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  presetMessage String []
  message String
  ttl DateTime
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
 }

 model FirebaseContractorNotification {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  presetMessage String []
  message String
  ttl DateTime
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
 }

 // protitipo
 model PrevDDSconfig {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  url String
  cloudflareConfig String [] @db.ObjectId
  tpcFirewall String [] @db.ObjectId
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
 }

 model MultiDeliverySystem {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  addressCompare GoogleAddressComponentType []
  addressSetMultyDelivery GoogleAddressComponentType []
  isActive Boolean @default(false)
  ttl DateTime
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
 }

model TrackingRaider {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  lastPosition PosMapType []
  livePosition PosMapType []
  isTracking Boolean @default(false)
  ttl DateTime
  createdAt DateTime @default(now())
  updateAt DateTime @updatedAt
}

model DeliveryAreaPlus {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  userId String @db.ObjectId
  radiusRangeCord CordType []
  location GoogleAddressComponentType []
  creatdAt DateTime @default(now())
  updateAt DateTime @updatedAt
  }

  model RaiderAppConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    config String [] @db.ObjectId
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model ContractorAppConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    config String [] @db.ObjectId
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model AdminDashboardConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    config String [] @db.ObjectId
    providerData String [] @db.ObjectId
    dbDriver String [] @db.ObjectId
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model BusinessDashboardConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    config String [] @db.ObjectId
    providerData String [] @db.ObjectId
    dbDriver String [] @db.ObjectId
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  } 
  // prototipo
  model ServerConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    config String [] @db.ObjectId
    service String [] @db.ObjectId
    providerData String @db.ObjectId
    osDriver String @db.ObjectId
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model Feedback {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    presetMessage String []
    message String
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model ValidationDelivery {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    deliveryId String @db.ObjectId // reference
    validation DeliveryValidation
    isValidate Boolean
    ttl DateTime
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt      
  }

  // prototipo
  model TsInterface {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    prototype String [] @db.ObjectId
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model StatsConfig {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    map String [] @db.ObjectId
    filter String [] @db.ObjectId
    reduce String [] @db.ObjectId
    data String [] @db.ObjectId
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model Geolocation {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    homeMobileCountryCode Int
    homeMobileNetworkCoode Int
    radioType String
    carrier String
    considerIp Boolean
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt
  }

  model gValidationAddress {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    address String 
    previousResponseId String
    enableUspsCass Boolean
    sessionToken String
    cratedAt DateTime @default(now())
    updateAt DateTime @updatedAt    
  }

  model gPlace {
    id String @id @default(auto()) @map("_id") @db.ObjectId
    type String
    formattedAddress String
    websiteUri String
    displayName String [] @db.ObjectId
    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
    }

    model gElevation {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      elevation String
      location String
      resolution String
      createdAt DateTime @default(now())
      updateAt DateTime @updatedAt
    }
  // prototipo
    model gReqPostRouteMatrix {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      origins gWaypoint
      destinations gWaypoint
      travelMode MetodoConsegnaEnum
      createdAt DateTime @default(now())
      updateAt DateTime @updatedAt  
    }
  // prototipo
    model gReqGetRouteMatrix {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      originIndex Int
      destinationIndex Int
      status String [] @db.ObjectId
      duration String
      condition Boolean
      cratedAt DateTime @default(now())
      updateAt DateTime @updatedAt
    }
    // prototipo 
    model gRouteGetDirection {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      distanceMeteres String
      duration String
      polylineEncode String // enum
      createdAt DateTime @default(now())
      updateAt DateTime @updatedAt
    }

    model qrValidation {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      validation String [] @db.ObjectId
      config String [] @db.ObjectId
      presetMultiDelivery String [] @db.ObjectId
      positionVal GoogleAddressComponentType []
      cratedAt DateTime @default(now())
      updateAt DateTime @updatedAt
    }

    model raiderTrackingHistory {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      history GoogleAddressComponentType []      
      createdAt DateTime @default(now())
      updateAt DateTime @updatedAt
    }

    // prototipo
    model raiderDeliveryStatusHistory {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      raiderStatusDelivery String []
      cratedAt DateTime @default(now())
      updateAt DateTime @updatedAt
    }

    // prototipo
    model contractorDeliveryStatusHistory {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      contractorStatusDelivery String []
      createdAt DateTime @default(now())
      updateAt DateTime @updatedAt      
    }

    // prototipo 
    model simpleApiConfig {
      id String @id @default(auto()) @map("_id") @db.ObjectId
      dbDriver String [] @db.ObjectId
      dbPath String [] @db.ObjectId
      dbConfig String [] @db.ObjectId
      cloudConfig String [] @db.ObjectId
      sApiConfig String [] @db.ObjectId
      createdAt DateTime @default(now())
      updateAt DateTime @updatedAt
    }
    // ***** Fine dataModel *****


// Note:
//  - add / fix references
//  - valutare flag stato consegna per ordini cancellati
//  - aggiungere sedi attvità
//  - aggiungere work time per filtri chiedere a fausto